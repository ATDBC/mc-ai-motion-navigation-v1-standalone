# B10-C 阶段验收记录：规划接纳与连续执行

日期：2026-09-23
状态：通过；八行接续矩阵、默认后台协调链和已发布能力回归均已完成当前冻结范围验收

## 本轮完成范围

本轮建立了下面这条正式链路：

1. 局部求解器从状态锚点生成真实命令、投影输入和预测轨迹；
2. 求解结果绑定规划请求、目标、路线位置、候选修订和风险策略；
3. `RouteAdmitter` 在执行前复核任务身份、状态入口、执行窗口、世界依赖和资源假设；
4. 执行器只接受已经接纳的候选，并按真实客户端输入回执逐 tick 前进；
5. 跨隙后的第一个转向可以写入同一份验证轨迹，随后在同一控制帧交给 Walk；
6. 迟到、错误 tick、目标变化、世界依赖变化和过期候选不会取得输入权限。

完整命令搜索已经有容量固定的 `MotionSolverWorker` 入口。控制侧提交和取结果都不等待求解。队列按动作连接保留任务，不用全局单槽把不同必要连接互相覆盖。后台结果如果已经落后于当前观察，接纳器会从最新状态重放同一命令序列；重放仍满足入口、途中碰撞、落点和资源条件时才生成新的证明。重放不搜索替代命令。

`MotionRouteCoordinator` 已接通活动路线、后台结果和路线执行器。`JumpGap` 现在默认要求经过该链；缺少证明时明确等待，不再静默退回旧控制器。历史 B09 回归只有在脚本中显式声明旧控制路径，才能绕过这项要求。

后台求解时客户端 tick 会继续前进。首条命令现在携带最早和最晚合法生效 tick；客户端回执给出真实生效 tick 后，执行器据此重新锚定剩余命令。后续命令仍要求连续、精确的逐 tick 回执。该规则只吸收候选交付延迟，不放宽动作中途的输入断档。

这轮没有扩大 B10-B 的入口范围。支持域仍是完整普通方块、低速站立入口、同高一格跨隙和四个正方向。落地后转向只支持一个正方向的首 tick。

## Fabric 结果

### 跨隙接纳和回执执行

通过产物：`artifacts/fabric-deployment/20260923T054319581177Z-c7ffd544`

引入后台队列和迟到结果重放后，使用当前代码复测：`artifacts/fabric-deployment/20260923T063950738376Z-5a9f5ab9`。复测仍为稳定落地 40／40、落地后立即转向 40／40，全部检查零失败；求解耗时 P95 2.714 ms，P99 和最大值 2.820 ms。该产物已把 `motion_candidate.py` 纳入运行前后源码哈希，覆盖实际使用的回执执行器。

| 项目 | 结果 |
|---|---:|
| 稳定落地正式样本 | 40／40 |
| 落地后立即侧向接续 | 40／40 |
| 四个方向 | 每个方向各 10 次 |
| 水平碰撞 | 0 |
| 稳定落地最大水平误差 | 0.0523 格 |
| 转向出口最大水平误差 | 0.1669 格 |
| 求解耗时 P95 | 2.712 ms |
| 求解耗时 P99 | 2.830 ms |
| 求解耗时最大值 | 2.830 ms |

每条命令都由 V3 输入应用回执确认。执行器没有根据“已经发送”推测动作完成。

### 已发布能力回归

| 范围 | 通过产物 | 主要结果 |
|---|---|---|
| Walk → JumpUp → Walk | `20260923T055027264461Z-bd56d68b` | 后台规划、接纳和整段执行通过；终点误差 0.1417 格 |
| 台阶和代表形状 | `20260923T055715385455Z-f073bc9e` | 16／16 完成；无跳跃脉冲、无水平碰撞；控制 P99 5.259 ms |
| Sprint、Crouch、Crawl | `20260923T060730262234Z-f4fd7217` | 30 条路线、取消、输入失联和 Crawl 退出全部通过；控制 P99 5.855 ms |
| JumpGap、ControlledDrop 和混合路线 | `20260923T061026518749Z-daef9379` | 80 个空中动作样本、两条规划混合路线和中断检查通过；控制 P99 4.716 ms |

这些结果证明本轮改动没有破坏 B05–B09 已发布范围。它们不能替代 B10-C 对每种新增接续方式的十次正式正例。

### 八行接续矩阵和默认协调链

当前代码沿用的正式通过产物：

- B05 JumpUp 与 Sprint 制动接续：`artifacts/fabric-deployment/20260923T081457004407Z-b100cc74`
- B07 连续台阶：`artifacts/fabric-deployment/20260923T081455440474Z-e4bdc7ce`
- B08 地面模式：`artifacts/fabric-deployment/20260923T081750045336Z-97399744`
- B09 空中动作和规划混合路线：`artifacts/fabric-deployment/20260923T081747621398Z-8fb6f40d`
- B10 默认后台协调链：`artifacts/fabric-deployment/20260923T083949813812Z-6348abdb`

| 接续 | 连续正例 | 边界对照 | 扰动／中断 |
|---|---:|---|---|
| Walk → Sprint | Fabric 10／10 | 食物不足、模式确认超时 | B08 取消和输入失联 |
| Walk → Step → Walk | Fabric 10／10 | 0.625 格高度正确拒绝 | Step 世界变化、取消和输入失联 |
| 低速 Walk → JumpUp → Walk | Fabric 10／10 | 位置、速度、方向入口有界 | JumpUp 空中取消和输入失联 |
| 低速 Walk → JumpGap → Walk | B09 规划路线 10／10；默认协调链 10／10 | 食物、落地区域、世界依赖和执行窗口检查 | B09 空中取消／失联；B10 旧候选拒绝 |
| Walk → ControlledDrop → Walk | Fabric 10／10 | 前后入口偏移和未知落点检查 | B09 空中取消和输入失联 |
| Step → Step | Fabric 10／10 | 每一级仍分别核验入口与高度 | Step 取消、输入失联和会话变化 |
| JumpGap 落地 → Walk 转弯 | 四方向 40／40 | 下一方向写入同一证明；错误连接拒绝 | 世界依赖变化和迟到候选重验 |
| Sprint 经预测制动 → JumpUp | Fabric 10／10 | 实际速度未进入 B05 范围时不跳 | 冲刺状态在制动时释放后仍保留交接责任 |

B07 连续台阶 10 次没有跳跃脉冲和水平碰撞，控制 P95 4.3141 ms、P99 5.2147 ms。B08 的 Sprint、Crouch、Crawl 各 10 次通过，控制 P95 5.5663 ms、P99 5.8873 ms。B09 的 80 个基础空中动作全部通过，另有跨隙和下降规划路线各 10／10，控制 P95 4.0932 ms、P99 4.6845 ms。

B10 默认协调器 10／10。同步稳定落地仍为 40／40，落地转向仍为 40／40。后台协调器每次都先产生明确等待，再交付动作；冷启动试次等待 34 次 5 ms 轮询，其余试次为 2 次。10 次最终水平误差均约 0.0522 格，无水平碰撞。同步求解 82 次的 P95 为 2.7674 ms，最大值为 3.3338 ms。

## 本轮发现并修正的问题

第一次跨隙转向运行 `20260923T053825375962Z-ffa6d2ea` 中，稳定落地 40／40 通过，但第一次转向预检失败。求解结果要求改变运动方向，实机脚本只发送了前进键。修正后，方向要求会在同一个控制帧转换成视角增量。

默认协调链增加后保留了三次失败产物。`20260923T082539535104Z-0b2497d0` 把前面基线的旧输入样本灌入新建的路线局部账本；现已只让局部账本接收本路线拥有的应用样本。`20260923T082909182490Z-23531801` 证明路线决策必须把证明命令编号和期望 tick 一起交给输入出口，不能由调用者猜测。`20260923T083302580358Z-a019b110` 证明后台求解期间客户端 tick 会前进，首条命令必须使用候选启动窗口和真实生效 tick 重新锚定。三次失败均保留，最终通过产物没有覆盖它们。

B07 首次回归 `20260923T055150474410Z-279de6fa` 暴露出一个旧检查过严的问题。它要求身体碰撞箱完整落在单个踏面内，导致玩家无法进入真实可站立的楼梯半格踏面。现在改为检查脚点属于声明踏面；通路和终点支撑继续由扫掠查询负责。修正后 16／16 通过。

B08 两次运行 `20260923T055858661725Z-bbeb661c` 和 `20260923T060249936975Z-c24f8dc2` 的运动检查都通过，但场景结束后整理大报告时，实时客户端仍持续产生未读取的输入采样，最终填满固定容量账本并崩溃。现在所有 B03–B10 单客户端运动验收都会先关闭实时会话，再离线整理报告。第三次运行完整通过，且没有残留进程或端口。

失败产物保留，不用后续成功覆盖。

## 组件和反例检查

组件测试覆盖：

- 目标 ID 或目标修订变化时，旧候选失效；
- 请求代次、路线修订、动作位置或候选修订变化时拒绝；
- 状态锚点已经前进、执行窗口过期或世界依赖变化时拒绝；
- 原始求解结果不能直接交给执行器；
- 输入在错误 tick 生效时不推进动作；
- 空中取消或输入失联后，执行器仍保留落地责任；
- 等待输入回执时不提交额外中性输入；
- Walk → Sprint、Step → Step 和 Sprint 制动 → JumpUp 的同帧交接契约；
- 狭窄到无法容纳身体的落地区域明确返回不支持。
- 后台进程能返回有界跨隙求解结果，且两个不同连接都能交付；
- 晚到结果必须在最新锚点重新验证，入口已经偏离时拒绝，不能沿用旧落点。

2026-09-23 审查整改后，当前运动导航专项检查为 327 项通过；客户端输入缓冲的原生 Java 检查为 1 项通过。迟到结果重放在同一进程内的测量只用于确认当前实现没有明显控制帧负担，不替代 Fabric 端到端时序验收。

### 审查整改后的 Fabric 回归

| 范围 | 通过产物 | 主要结果 |
|---|---|---|
| B04 已知地图 | `20260923T123337232268Z-b0f1c8b6` | 空地、高墙、悬浮墙、坑 4／4；障碍场景规划约 5.9–6.0 ms |
| B07 台阶与形状 | `20260923T121926115655Z-e4916fd4` | 16／16；连续台阶 10／10；无跳跃脉冲和水平碰撞 |
| B09 空中动作 | `20260923T121930431947Z-2c742d5c` | 80／80；跨隙和下降规划路线各 10／10；两种中断均安全结束 |
| B10 求解与协调 | `20260923T121928785965Z-fe5a73d4` | 稳定落地 40／40；落地转向 40／40；默认协调 10／10 |
| 输入缓冲长空闲 | `20260923T124607894932Z-f869c1cb` | 空闲 10.500 秒；累计 212 个采样，保留 64 个，报告丢弃 147 个；随后前进和释放输入都被真实 tick 确认 |

B07、B09、B10 同时运行，用于检查整改后的行为是否退化。B10 求解 P95 为 7.447 ms，最大 8.751 ms；该数据包含三场 Fabric 并行负载，因此只作为本轮正确性回归的时序记录，不替换前面单独运行得到的正式性能成绩。B04 和输入缓冲专项随后分别单独运行。

输入缓冲第一次专项运行 `20260923T124255844198Z-8bdbad4f` 没有覆盖目标条件：测试在机器人输入对象安装前开始空闲，所以采样计数没有增长。它还把有效零输入误写成 `leased`，实际协议状态是 `neutral`。失败产物保留。最终测试先通过正式仲裁安装机器人输入，再开始空闲，因此直接覆盖“消费者超过 64 个运动 tick 没有取回执”的情况。

### 表面规划性能修正

正式 `plan_known_surface_snapshot` 入口按需查询支撑列和动作边，不先构造整个 `SurfaceGraph`。审查整改后的基准覆盖三种平地终点和一张 20×20 折返迷宫。在 100×100 平地上，对角终点展开 198 个节点，偏轴终点分别展开 132 和 123 个节点；20 次运行的 P95 分别为 87.1483、74.6826 和 51.2620 ms。20×20 折返迷宫展开 266 个节点，5 次运行 P95 为 222.3044 ms。四种情况都在 500 ms 规划预算内。

这项结果没有证明任意规模迷宫都能在 500 ms 内完成。大型蛇形地图仍可能受逐列几何查询影响而超时；发生时应由规划预算返回明确超时，不能把本轮小迷宫结果外推成无界性能声明。

复现命令：

```powershell
D:\Miniforge3\Scripts\conda.exe run --prefix D:\My_project\mc_ai\.venv --no-capture-output python scripts\benchmark_surface_planner.py --size 100 --runs 20 --output output\motion-nav\b10\surface-planner-100x100.json
```

本轮还保留了物化表面图和独立 Dijkstra，用于小图等价性检查。性能结论只覆盖这张冻结平地图；混合动作和资源约束由后续的多动作图验收分别记录。

## 结论和边界

### D026 二次复审后的完整回归

当前代码对应的正式运行是 `artifacts/fabric-deployment/20260925T123521898324Z-5d0a66dc`：

- 稳定跨隙 40／40、落地转弯 40／40、三档带速入口 60／60、默认后台协调 10／10；
- 210 个协调控制帧没有一次输入晚于候选许可窗口；探针只读取 Runtime 的输入账本，中性落地帧没有再写入第二份局部账本；
- 142 次求解的 P95 为 24.1324 ms、P99 为 24.9028 ms、最大值为 27.8626 ms，候选数仍保持原上限；
- 相同碰撞形状查询在一次求解内复用结果。活的世界视图仍会在每次复用前核验依赖分区版本，地形变化后不能继续使用旧形状；
- 所有部署检查、物理 tick 记录、源码哈希和进程清理通过。

两次性能失败继续保留。`20260925T120140167577Z-9c5c0fd5` 与另一场游戏并行，最大求解超过门槛；随后独立运行 `20260925T121313766119Z-50b8e86d` 的最大值仍为 34.5117 ms，证明不能只归因为并行负载。第一次缓存修正后的 `20260925T122647304689Z-90078ae4` 把 P95 降到 25.8332 ms，但最大值仍为 30.5406 ms，因此也按失败保留。最终版本批量核验分区修订并复用碰撞形状，才在不放宽 30 ms 门槛的情况下通过。

B10-C 和整个 B10 在冻结范围内通过。普通已知地形规划可以保留同一对支撑面之间的不同动作边，并用包含模式、姿态和速度区间的 `PlannerStateKey` 避免错误合并。当前动作资格不依赖来向，所以方向没有进入状态键；以后出现方向相关资格时必须补回对应区别。`JumpGap` 的默认执行链已经是后台求解、当前状态接纳和回执驱动执行。

本结论不包含带速起跳、Sprint 直接进入 JumpGap、主动 Crawl、攀爬、流体、特殊接触、未知探索或动态实体。矩阵以外的新组合仍须先声明入口、出口、时序和证据。
