# B09 验收：参数化跳跃与受控下降

日期：2026-09-19

状态：已通过

## 场景

| 场景 | 地形 | 必须证明 |
|---|---|---|
| B09-S01 | 普通一格上升 | B05 `JumpUp` 回归不退化 |
| B09-S02 | 同高平台之间的一格空隙 | `JumpGap` 经实际离地和目标支撑确认完成 |
| B09-S03 | 相邻的一格低台 | `ControlledDrop` 不按跳跃键并落到目标层 |
| B09-S04 | Walk—跨隙—Walk | 后台选路、接纳和动作执行闭环 |
| B09-S05 | Walk—下降—Walk | 后台选路、接纳和动作执行闭环 |
| B09-S06 | 轨迹碰顶、未知净空、未知落地、错误层 | 分别阻止启动或明确失败 |
| B09-S07 | 离地前后取消和输入失联 | 地面可停，空中先安全落地 |

## 门槛

- 每类新动作四个正方向各至少 10 次，声明范围内成功率 100%。
- 无错误层完成、无落入空隙、无碰撞、无预计伤害。
- 默认风险策略只接受零伤害轨迹。
- A* 的完成路线代价与独立 Dijkstra 一致。
- 控制决策 P95 不超过 8 ms，P99 不超过 15 ms，最大值小于 30 ms。
- 全部 `tests/motion_nav` 通过；B05、B07、B08 代表性 Fabric 场景继续通过。

## 结果

正式 B09 Fabric 验收产物：`artifacts/fabric-deployment/20260919T162705802883Z-d13e291e`。

- `JumpGap` 四个正方向各 10 次，共 40/40 完成；最大落点中心误差为 0.0622 格。
- `ControlledDrop` 四个正方向各 10 次，共 40/40 完成；最大落点中心误差为 0.2357 格，40 次均未按跳跃键。
- 80 次均落到正确层、没有水平碰撞，也没有落入空隙。
- 后台进程生成并执行了 `Walk—JumpGap—Walk` 与 `Walk—ControlledDrop—Walk`。两条路线均通过接纳并完成，最终误差分别为 0.1182 格和 0.0397 格。
- 离地后取消最终报告 `CANCELLED`，输入失联最终报告 `INPUT_LOST`；两者都先沿校准输入安全落地。
- 控制决策共 1,240 个样本，P95 为 4.121 ms，P99 为 4.728 ms，最大值为 4.799 ms。
- A* 与独立 Dijkstra 的组件对照一致。负高度整数接触面的扫掠回归也已覆盖。

运行命令：

```powershell
D:\Miniforge3\Scripts\conda.exe run --prefix D:\My_project\mc_ai\.venv --no-capture-output python scripts/probe_fabric_deployment_observation.py --b09-air-motion-probe --timeout-seconds 300
```

前序代表性 Fabric 回归也已通过：

| 阶段 | 产物 | 结果 |
|---|---|---|
| B05 | `artifacts/fabric-deployment/20260919T163044141056Z-35fe5cde` | 17 项检查全部通过 |
| B07 | `artifacts/fabric-deployment/20260919T163041662833Z-57087815` | 22 项检查全部通过 |
| B08 | `artifacts/fabric-deployment/20260919T163043513486Z-fca97701` | 17 项检查全部通过 |

校准失败证据没有删除。早期批次分别暴露了观察请求过大、落地支撑未正式观察、跨坑松键过早、下降离边前减速过晚、负高度接触误判和混合路线入口范围不真实等问题。最终 Profile 只声明正式通过的范围。

当前边界：只覆盖普通地面、同高一格空隙和相邻一格零伤害下降；入口为低速稳定状态。对角跨隙、多格跨越、带速连续接续和更大落差仍未授权。

## 2026-09-20 审查补充

跨隙动作新增明确的最低饥饿值，并在规划、路线接纳和离地前控制三个位置复核。输入确认在 `PREPARE` 丢失时不会再发出跳跃。表面路线现在保留目标状态与资源状态，终点模式或姿态不相容时规划直接拒绝。

自动检查结果为运动／导航 211 项全部通过，观察协议 19 项全部通过，Fabric 探针重新构建成功。实机回归产物为 `artifacts/fabric-deployment/20260920T040111313432Z-7d2a3f6a`：跨隙 40/40、受控下降 40/40 完成，两条混合路线完成；控制计算 P95 为 4.1551 ms，P99 为 4.7466 ms，最大为 5.5086 ms。
