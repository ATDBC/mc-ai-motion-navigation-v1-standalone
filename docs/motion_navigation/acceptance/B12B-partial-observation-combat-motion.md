# B12-B 验收计划：部分观察与战斗移动

日期：2026-09-26
状态：正式验收与审查整改均已通过

## 1. 组件检查

至少覆盖：

1. 普通地面路线在战斗视角稳定时，可以分别用前进、左右横移和后退继续走；
2. 战斗视角先确定，普通地面移动按该视角重新计算，并绑定具体视角意图；
3. 绑定视角胜出时，大幅转头可以继续执行重新验证过的移动；绑定视角落选时只压下本帧移动；
4. 台阶、上升、跨隙和下落没有显式许可时，不能使用普通地面的规则；
5. 目标当前不可见但交战位置有效时，可以继续接近；进入攻击距离后必须先补看；
6. 单帧观察缺口不清空仍有效的路线执行器；
7. 未建立交战许可、实体卸载、位置过期、目标修订和世界变化都会撤销旧权限；
8. 移动、视角、攻击的在线选择能够从正式记录重放。

## 2. Fabric 场景

场地使用已知普通平地和一面能够暂时遮住目标的实墙。每个场景在运行前固定世界种子、僵尸 AI 种子、起点、目标方向和路线。

正例冻结为 24 次：

| 场景 | 次数 | 要证明什么 |
|---|---:|---|
| 面向目标前进并攻击 | 8 | 普通直线组合没有回归 |
| 面向目标横移并攻击 | 8 | 左右横移由路线控制器自动选择，移动不被战斗视角压掉 |
| 面向目标后退并攻击 | 8 | 后退仍沿安全路线，攻击不取得身体控制权 |

边界和反例冻结为 12 次：

| 场景 | 次数 | 预期 |
|---|---:|---|
| 首帧需要大幅转向目标 | 2 | 按最终视角重算移动，转头期间继续安全行走，不复用旧朝向输入 |
| 已交战目标短暂被墙遮挡 | 2 | 继续使用新鲜位置导航，不能隔墙攻击 |
| 遮挡后进入攻击距离 | 2 | 停止无意义攻击并补看，重新可见后继续 |
| 单帧观察不连续 | 2 | 原身体负责人继续；新攻击等待连续观察 |
| 未交战目标在墙后 | 2 | 不使用隐藏位置，不产生攻击 |
| 世界或目标修订变化 | 2 | 旧路线、瞄准和攻击失效，不跨会话复用 |

有效可达正例若超时或停顿，仍保留在分母中。夹具本身没有生成目标、种子未应用或世界重置失败时，单独记为夹具无效。

## 3. 指标

- 24／24 个正例完成；
- 12／12 个边界和反例得到预先声明的结果；其中真实 Minecraft 事实 8 次，确定性控制链注入 4 次；
- 已验证路线走廊外偏离、坠落和墙体碰撞均为 0；
- 每种移动方向都在实际应用记录中出现，不能只根据计划宣称；
- 控制帧计算沿用 P95 不超过 8 ms、P99 不超过 15 ms；
- 正式记录丢弃 0，在线与离线重放一致。
- 活动目标始终记录驱动启动到首条非中性移动命令的原始时间，以及首动前控制帧数；已有信息足以形成路线时，原始时间不超过 0.5 秒；若会话明确等待未知地形，则最后一帧“信息不足”结束后不超过 10 个控制帧开始移动。实际输入生效仍按输入回执单独核对。

## 4. 回归

提交前运行运动／导航、Runtime、仲裁、固定近战、移动近战、外力恢复、B11 和 B12-A 检查。实机验收后再重跑一组 B10 跨隙，确认普通地面的新权限没有进入空中动作。

## 5. 完成条件

组件、Fabric 正反例和 B10 跨隙回归全部通过后，才关闭 B12-B。组件通过不能写成战斗移动已经实机成立。

## 6. 正式结果

### 6.1 Fabric 行为与性能

正式批次：`artifacts/fabric-deployment/20260926T025406298506Z-5bf010ce`。

运行命令：

```powershell
D:\Miniforge3\Scripts\conda.exe run --prefix D:\My_project\mc_ai\.venv --no-capture-output python scripts/probe_fabric_deployment_observation.py --b12b-partial-combat-probe --time-diagnostics --seed 21001 --server-port 25617 --ipc-port 8160 --timeout-seconds 900
```

结果：

- 24／24 个正例通过：前进 8、后退 8、左移 4、右移 4；
- 6／6 个依赖游戏事实的边界场景通过：大幅转头、遮挡后导航、攻击距离内补看各 2 次；
- 每个正例都在同一个真实客户端 tick 中应用路线移动并提交攻击，且后续确认命中；
- 隔墙攻击、错打实体、路线记录丢失均为 0；
- 889 个控制帧的决策耗时为 P50 1.7920 ms、P95 5.0015 ms、P99 6.2107 ms、最大 12.2116 ms；
- 正式记录写入 3136 条，丢弃 0 条，记录线程没有失败；
- 归档冻结了实际使用的移动近战、交战记忆、导航、仲裁和 Runtime 源码，运行前后哈希一致。

场景中的僵尸使用 `NoAI`，因此不存在未固定的移动随机性。清单仍保存世界种子 `21001` 和每次试次的场景种子。该结果只证明单目标、普通平地和当前近战门禁，不外推到活动 AI、多敌人、台阶或空中动作。

复审逐帧核对后确认，这批正例的战斗转角均为 0。六个非零转头帧都属于 15° 大幅转头边界，移动按预期被压下。因此，上述结果证明“移动与攻击同帧”，不证明“非零转头与移动同帧”。后者按 D032 重新实现并补充活动目标实机证据后，才能关闭。

这批整改前记录仍保留在本地原始实验目录，用来说明当时只验证了固定视角下的组合。公开精简包已更新为本页第 8 节的新批次，避免审查者误把旧结论当成当前最终证据。

### 6.2 确定性运行时注入

运行命令：

```powershell
D:\Miniforge3\Scripts\conda.exe run --prefix D:\My_project\mc_ai\.venv --no-capture-output python -m unittest tests.test_b12b_runtime_injection_acceptance -v
```

共 6 个场景，全部通过：

- 单帧观察缺口 2 次：原导航负责人继续，连续观察恢复前不增加攻击；
- 未获交战许可的隐藏目标 2 次：不授予隐藏位置使用权，不提交攻击；
- 目标修订 1 次、世界会话变化 1 次：旧动作失效，不跨修订或会话复用。

### 6.3 B10 跨隙回归

正式批次：`artifacts/fabric-deployment/20260926T023301334136Z-47dfd7ea`。

静止入口 40／40、转向出口 40／40、带速入口 60／60、默认后台协调 10／10 全部落地成功；24 个带速反例按预期拒绝。普通地面的 5° 单帧许可没有进入 `JumpGap`，跨隙仍走动作证明、后台求解和输入回执链。

### 6.4 组件与跨模块回归

- `tests/motion_nav`：455／455 通过；
- Runtime、仲裁、B11、B12、交战记忆、近战和实机探针相关检查：209／209 通过；
- B12-B 确定性注入：6／6 个场景通过，包含在上述相关检查中。

## 7. 诊断过程与证据边界

早期批次按失败保留，不纳入正式成功率：

- `20260926T020553094790Z-ad4bc71e` 暴露测试目标没有对齐支撑面代表点；
- `20260926T021513724063Z-b7eecd50` 暴露导航在启动时失败后，移动近战仍调用已经终止的驱动；
- `20260926T021859431824Z-3b1c9684` 暴露两种遮挡场景使用了同一距离；
- `20260926T022321197115Z-a9d35247` 暴露补看开始后，目标移出攻击距离仍不会切回导航。
- `20260926T024417432199Z-714575a5` 的行为和性能均通过，但源码冻结清单漏了移动近战与交战记忆文件，因此由完整冻结的正式批次替代。

这些问题均先加入失败检查，再作最小修正。正式批次没有删除或重分类任何有效试次。

## 8. 审查整改后的正式结果

正式批次：`artifacts/fabric-deployment/20260926T063032338012Z-780e0386`。

公开精简证据位于 `evidence/motion_navigation/representative-v1/archives/b12b-pass-20260926T063032338012Z-780e0386.tar.gz`。它保留 33 个 Fabric 场景、控制事件、分段 Runtime 轨迹和源码哈希，不含世界、普通日志、画面或 Minecraft 依赖。

- 原 24 个固定目标正例继续全部通过；
- 新增 1 个活动目标专项。僵尸保留原版 AI，随机种子固定为 `51001`。导航产生移动后，夹具把同一目标横移到侧前方，稳定触发持续转头；正式 `MovingMeleeDriver` 产生两个转头与移动组合请求；
- 客户端实际应用时，视角在 tick N 应用，移动输入在紧接着的运动 tick N+1 消费。两者拥有相同请求号和时间事件序列，中间没有其他时间事件。该结果表示同一控制应用区间，不能把不同钩子的计数写成完全相同的 tick；
- 大幅转头边界两次各出现 4 帧“15° 转头＋非空移动”，移动压下次数为 0；
- “未交战目标在墙后”改为真实石墙场景，两次都确认目标已从可见实体列表消失，隐藏位置使用、移动和攻击均为 0；定向查询槽保留的旧值仅作诊断，不授予位置使用权；
- 其余墙体遮挡、补看和交战位置场景继续通过。8／8 个 Fabric 边界没有失败；
- 共 33 个 Fabric 场景完成。控制计算 P50 1.8767 ms、P95 4.4011 ms、P99 5.7083 ms，所有检查通过。

确定性 Runtime 注入缩减为 4 次：单帧观察缺口 2 次，目标修订和世界会话变化各 1 次。“未交战目标在墙后”不再用注入代替实墙；原注入组件测试仍保留，用于检查权限消费者。

跨阶段回归：

| 能力 | 批次 | 结果 |
|---|---|---|
| C1-B 活动目标连续近战 | `20260926T063405450804Z-646f449c` | 20／20 正例、10／10 反例；控制 P95 6.5766 ms、P99 7.7832 ms |
| B03 固定路线 | `20260926T063850514146Z-82a6a76d` | 25 条路线通过，途中低速停滞为 0，取消和输入失联按原契约收敛 |
| B10 跨隙 | `20260926T064258873497Z-780a4000` | 静止 40／40、落地转向 40／40、带速 60／60、默认协调链 10／10；输入没有晚于许可窗口；求解 P95 24.187 ms、P99 25.924 ms |

此前 `20260926T025406298506Z-5bf010ce` 的范围和结论不改写。它仍证明固定视角下的移动与攻击同帧；本节的新批次补齐持续瞄准移动与真实墙体证据。

## 9. 第十轮审查整改与持续追击结果

正式批次：`artifacts/fabric-deployment/20260926T074601049372Z-1e50bcb7`。

公开精简证据位于 `evidence/motion_navigation/representative-v1/archives/b12b-pass-20260926T074601049372Z-1e50bcb7.tar.gz`。它替代第 8 节批次成为当前 B12-B 公开证据，但不改写旧批次的历史结论。

本轮增加了两项检查：

1. 导航把每帧的移动决定和原因写进正式任务事件。记录能区分正常跟踪、转弯控速、取消制动和停止后的取消。
2. 增加一场不瞬移目标的持续追击。战斗在追逐阶段持续给出目标视角，导航再按该视角计算普通地面移动。

结果如下：

- 24／24 个固定目标正例和 8／8 个 Fabric 边界继续通过；
- 人工诱发转向场景运行 52 个控制帧。8 个转头帧中有 7 个同时移动，目标累计移动 2.920 格；
- 持续追击场景运行 43 个控制帧，共 2.150 秒。目标使用原版 AI，自行移动 2.056 格，没有目标瞬移；15 个转头帧中有 9 个同时移动，占 60%；
- 两场活动目标均确认命中。持续追击记录了 30 帧正常路线跟踪、5 帧转弯控速、5 帧取消制动和 1 帧停止后的取消；
- 共完成 34 个 Fabric 场景。1026 个控制帧的计算耗时为 P50 1.9408 ms、P95 4.7440 ms、P99 6.4896 ms、最大 9.3371 ms；
- 客户端伤害事件缓冲在目标尚未建立追踪编号时会立即丢弃该条无消费者事件，不把它算作容量溢出。70 轮“不可见实体环境受伤＋玩家命中”组件检查中，70 次玩家命中全部送达，容量丢弃保持 0。

第十轮审查指出原 B03 实机脚本直接驱动地面控制器，没有经过 `NavigationSession`。因此，第 8 节的 B03 结果只称为“地面控制器回归”，不再用它证明一帧步行授权。正式 `NavigationSession` 路径由本轮持续追击和下面的 C1-B 回归覆盖：

| 能力 | 批次 | 结果 |
|---|---|---|
| B12-B 持续追击 | `20260926T074601049372Z-1e50bcb7` | 34／34 个 Fabric 场景通过；持续追击 15 个转头帧中 9 个同时移动 |
| C1-B 活动目标连续近战 | `20260926T074903856184Z-9fa68417` | 20／20 正例、10／10 反例；控制 P95 6.4855 ms、P99 7.3977 ms |
| B03 地面控制器 | `20260926T063850514146Z-82a6a76d` | 25 条路线通过；只证明地面控制器，不证明 `NavigationSession` 的一帧授权 |

开发过程中保留两次有效失败：`20260926T073444198265Z-10af0d4a` 和 `20260926T073823846129Z-0ec5f129`。两次都证明仅改变起始距离不能解决目标走出视野；根因是追逐阶段没有持续给出战斗视角。失败没有从正式成功率中删除，只是属于修正前代码，未并入修正后冻结批次。

当前证据仍有边界。持续追击只有一个固定种子和一只僵尸，不能代表多目标、复杂地形或目标已经离开视野后的主动重获。伤害来源的不同实体样本仍主要来自组件测试；这些能力以后按各自阶段补实机证据。

第十轮审查进一步发现，旧批次在路线接纳前没有会话级原因，人工诱发转向场景从目标确定到首条移动用了约 1.39 秒。因此，本节关于中性帧原因的统计只对已有路线后的帧成立。D034 已补齐会话级记录、首动指标和批次内后台进程复用；新的 Fabric 结果写入下一节，旧批次不改写。

## 10. 第十一轮审查整改结果

正式 B12-B 批次：`artifacts/fabric-deployment/20260926T091904725912Z-deb6726e`。

公开精简证据位于 `evidence/motion_navigation/representative-v1/archives/b12b-pass-20260926T091904725912Z-deb6726e.tar.gz`。同一语料库中的 B10-C 和 C1-B 通过批次也已更新为本轮回归，历史失败批次继续保留。

本轮补齐了无路线帧原因、追击期限的任务层收尾、批次内后台进程复用，以及普通步行与非步行动作的视角归属。结果如下：

- 24／24 个固定目标正例、8／8 个 Fabric 边界和 2／2 个活动目标场景通过；
- 人工诱发转向场景从驱动启动到首条移动用了 1.505 秒。首动前 29 帧中，10 帧为目标支撑面信息不足，15 帧为已知地图内没有路线，4 帧为后台规划。最后一次信息不足结束后，第 2 个控制帧开始移动；
- 上述等待不能归因于计算量。缺失位置是一排被遮挡的实体地面。导航层只允许无条件确认空气，不能把未看到的实体方块直接判成可走；目标接近已知地面后路线成立；
- 持续追击场景的原始首动时间为 0.251 秒，首动前 4 帧中有 2 帧等待目标支撑面、2 帧等待规划；最后一次信息不足结束后，第 2 个控制帧开始移动；
- 人工诱发转向的 8 个转头帧中有 6 帧同时移动；持续追击的 14 个转头帧中有 9 帧同时移动；
- 873 个控制帧的计算耗时为 P50 1.7951 ms、P95 4.6696 ms、P99 5.9228 ms、最大 8.9483 ms；
- 每个正式试次内的中性导航帧都有会话级原因。统计不读取试次结束后的取消和制动来解释试次内停顿。

追击达到 30 秒任务期限时，不再抛出程序契约异常。导航先安全停止或完成落地，再以 `NEEDS_TASK_DECISION` 和 `pursuit_deadline_exhausted` 交给任务层。

视角归属固定为：普通 `WalkSegment` 可以使用本帧胜出的战斗视角重新计算按键；台阶、上升、跨隙和下落保留路线动作自己的视角。该规则已经由组件检查覆盖，并由下列实机回归确认没有影响现有能力：

| 能力 | 批次 | 结果 |
|---|---|---|
| C1-B 活动目标连续近战 | `20260926T090526982665Z-de19f7bc` | 20／20 正例、10／10 反例；P95 6.5067 ms、P99 7.9638 ms |
| B10 跨隙 | `20260926T091031597407Z-f7f68346` | 静止 40／40、落地转向 40／40、带速 60／60、默认协调 10／10；24 个反例按预期处理；求解 P95 24.2813 ms、P99 27.8718 ms |

本轮没有实现未知区域探索。原始首动时间仍完整保留，不能因为属于信息等待就从报告中删除。10 帧门槛只检查信息已经足够后，控制链是否及时开始行动。
