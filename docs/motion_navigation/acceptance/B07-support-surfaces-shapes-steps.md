# B07 验收：支撑面、非完整形状与小台阶

日期：2026-09-19
状态：通过

## 判定原则

B07 只有在几何、规划、执行和 Fabric 实测形成闭环后才算通过。单独识别碰撞盒、生成路径或播放演示都不能代替闭环。

## 强制场景

| 场景 | 预期结果 | 证据 |
|---|---|---|
| 下半砖连续路面 | 生成真实半格高度站位并连续通过 | Fabric `continuous_lower_slab` 成功，水平终点误差 0.143 格 |
| 上半砖与完整方块连接 | 使用正确整数顶面，不误判身体净空 | Fabric `upper_slab_level` 成功，水平终点误差 0.143 格 |
| 直楼梯上／下 | 使用 Step 转换完成 | Fabric 上、下各 1 次，均完成 |
| 转角楼梯 | 按实际组合碰撞建立连通，不穿过封闭部分 | Fabric 上、下各 1 次，均完成 |
| 地毯与土径 | 保留真实高度并完成普通步行 | Fabric 各进入、离开 1 次，均完成 |
| 多层积雪 | 已校准层数通过，边界层数正确拒绝或改用其他能力 | 2、4、5 层各进入、离开 1 次；6 层产生 0.625 格高差并返回 `UNSUPPORTED` |
| 栅栏和墙 | 越出本格的碰撞能够阻挡，并记录所有者依赖 | Fabric 实际碰撞扫掠均为 `BLOCKED`；墙高 1.5 格 |
| 薄障碍和活板门 | 开／关状态按实际碰撞处理 | 铁栏杆及开／关活板门均读取实际盒；打开的活板门正面阻挡、沿空侧放行 |
| 同列双表面 | 两个高度分别建点，不合并 | 直楼梯和转角楼梯均在同列保留两层踏面 |
| 未知邻格可能伸入 | 返回 `NEEDS_INFORMATION`，不能放行 | 组件反例通过 |
| 耕地 | 几何可读，动作因风险未授权保持拒绝 | Fabric 读取 15/16 高表面；分类仍为 `deferred/world_change_risk` |

## 回归场景

- B06 五种普通完整方块的 Walk 和 JumpUp。
- B04 已知高墙、悬浮墙和坑绕行。
- B03 直线、转弯、停止、取消和输入失联。
- 世界会话变化、旧路线覆盖和依赖变化撤销。

## 指标

| 指标 | 门槛 |
|---|---|
| 强制场景 | 全部完成或按表中原因拒绝 |
| 可避免碰撞／跌落 | 0 |
| 未知放行 | 0 |
| 错误完成 | 0 |
| 小图规划 | 与独立 Dijkstra 的最小代价一致，误差不超过 `1e-6 × max(1, reference_cost)` |
| 控制计算 | P95 ≤8 ms、P99 ≤15 ms、最大 <30 ms |
| 旧能力回归 | B02–B06 专项测试全部通过 |

Fabric 样本数、种子和场景坐标在首次正式运行前冻结。组件、闭环模拟和 Fabric 次数分别报告，不能互相代替。

## 证据边界

### Fabric 正式运行

命令：

```powershell
D:\Miniforge3\Scripts\conda.exe run --prefix D:\My_project\mc_ai\.venv --no-capture-output python scripts/probe_fabric_deployment_observation.py --b07-step-probe --seed 21003 --server-port 25666 --ipc-port 8209 --timeout-seconds 600
```

原始证据：`artifacts/fabric-deployment/20260919T140738400900Z-7dcd37a2`

结果：22 项检查全部通过。16/16 次 Step 完成，跳跃输入 0，水平碰撞 0，最大高度误差 0。最大水平终点误差 0.179 格。控制计算共 240 个样本，P95 为 4.290 ms，P99 为 5.209 ms，最大为 5.502 ms。另有上半砖同高步行和连续下半砖步行各 1 次，均成功。

场景命令只建立和重置测试地形、传送到动作入口。碰撞形状、空气、身体状态、动作完成和失败判断都来自正式 Fabric 观察与动作入口。试验没有使用图像或测试真值给规划器补答案。

### 组件与回归

```powershell
D:\Miniforge3\Scripts\conda.exe run --prefix D:\My_project\mc_ai\.venv --no-capture-output python -m unittest discover -s tests/motion_nav -p 'test_*.py'
```

结果：168 项通过。表面 A* 与独立 Dijkstra 的最小代价一致；旧 Walk、JumpUp、固定路线、已知图后台规划、取消、输入失联和世界会话测试均通过。

主要实现提交：`684e3fc`（表面路线、后台快照、JumpUp 复用）和 `9b38952`（Fabric 代表形状与 StepDown 落地修正）。此前的跨格所有者、表面节点、Step 查询和动作路线提交仍保留在同一分支历史中。

### 失败证据与修正

- `20260919T135121602348Z-8d68ab27`：探针重构遗漏局部变量，未产生能力结论；修正后重跑。
- `20260919T135259682536Z-07306a2f`：直楼梯下行在离地后反向修正，重新爬回高踏面；测试夹具还可能在同位置传送时提前接受旧朝向。执行器改为下行进入目标水平范围且离地后松键等待落地，夹具增加朝向同步条件。
- 最终运行确认两项修正后，直楼梯、转角楼梯上下均完成。

### 仍未授权的范围

B07 只证明静止、低速入口下不超过 0.5 格的普通步行升降。它没有授权带速连续接续、疾跑、潜行、跨坑、主动跳下、特殊地面动力学、流体、攀爬和未知探索。活板门只按当前碰撞状态判定，机器人尚不会主动操作活板门。
