# B10 验收计划：动作求解与连续执行

日期：2026-09-21
状态：验收原则、最低数量和统计规则已冻结；具体场景 ID、参数和种子须在首次正式运行前冻结；尚无 B10 实现结果

具体场景可以维护在本文表格或本文引用的机器可读清单中，只保留一个权威来源。B10-C 阶段文档只引用该清单，不另行复制。

## 1. 证据分层

每项能力分别记录：代码实现、组件验证、固定输入 Fabric 差分、自动求解 Fabric 验证、规划接入和组合闭环。前一层通过不能替代后一层。

证据按能力维护。只有试点实际使用的规则分支需要在 B10-A 前补齐；其他分支在对应能力启用前完成。测试分组在运行前冻结。所有失败、超时和无法复现结果都保留；预先声明为有效可达的任务不能根据运行结果移出成功率分母。

## 2. B10-A 验收

### 必须通过

- `+Z` 草方块同高一格跨隙形成连续的命令—投影输入—运动前状态—运动后状态证据链；
- 状态锚点包含会话、观察、运动 tick、相位和已确认控制；
- 在途输入账本有固定容量，并覆盖应用后回执迟到、重复回执、过期和无法消除歧义；
- 诊断开关不改变正式 actor 行为；
- 投影结果可以由真实命令产生；
- 低顶、未知净空、未知落点、旧会话和过期窗口正确拒绝；
- 固定 1／20／60 tick 和小批量候选基准记录完整检查耗时。

### B10-A 不证明

- 自动找到起跳时机；
- 普通任务可以使用新链路；
- 全部普通材质、复杂形状、Crawl 转换或任意方向已经通过。

## 3. B10-B 验收

### 正例

- 四个正方向各至少 10 次独立 Fabric 运行，共至少 40 次；
- 起点、入口扰动和求解验证样本在运行前冻结；
- 全部落到正确层和目标区域；
- 没有水平碰撞、落入空隙、预计伤害或后验输入；
- 求解结果完整保留命令、投影输入、轨迹、入口—出口关系和依赖。

### 反例

- 途中净空不足；
- 落点或恢复范围不足；
- 食物值不满足冲刺资格；
- 入口速度、方向或位置超出范围；
- 世界依赖变化；
- 预算耗尽；
- 未支持规则；
- 求解得到的模拟输入不能由正式命令产生。

预设的不可执行、缺信息和预算耗尽注入用例，单独验收结果分类，不进入有效可达任务成功率。有效可达任务在正常预算内未完成的，必须保留在分母中，并按失败或超时计数，不得根据运行结果重新分类。

某次内部求解用尽预算，但任务在允许的恢复次数和总期限内最终完成，可以记为任务成功；仍须另外记录该次求解失败、恢复次数、耗时和资源代价。

## 4. B10-C 接续矩阵

正式场景清单必须逐行给出入口域、中间状态、出口、是否允许减速、时序和场景 ID。首版覆盖：

- Walk → Sprint；
- Walk → Step → Walk；
- 低速 Walk → JumpUp → Walk；
- 低速 Walk → JumpGap → Walk；
- Walk → ControlledDrop → Walk；
- Step → Step；
- JumpGap 落地 → Walk 转弯；
- Sprint 经预测制动 → JumpUp。

每行至少包含连续正例、边界对照和扰动／中断。每个确定性正例默认重复 10 次；空中动作的四方向要求仍分别计算。若实现前需要改变次数，必须先写新的决策记录，不能看到结果后再改门槛。

B10 总完成要求是矩阵中所有声明行通过。“至少一组无强制停稳”只可作为 B10-C 的早期里程碑。

## 5. 计时口径

分别报告：

1. 冷启动／首次路线，包含第一次加载和准备；
2. 按固定预热步骤得到的稳态计算；
3. 状态采样到观察包就绪的观察年龄；
4. 从观察包就绪到控制提交完成的控制进程耗时；
5. 从控制提交到客户端实际应用的延迟，以及实际应用的玩家运动 tick；
6. 从状态采样到输入实际应用的完整年龄，并核验应用时仍处于候选执行窗口。

稳态控制进程完整本帧继续使用现有门槛：P95 不超过 8 ms，P99 不超过 15 ms，名义最大值小于 30 ms；50 ms 提交期限单独统计，名义 10 分钟运行期限失败为 0。本机状态采样到输入实际应用继续继承 P99 不超过 100 ms 的门槛。

每个原始帧是一个样本。记录样本数和分位数算法，不把多次运行各自的 P99 再平均。`perf_counter` 记录实际经过时间；线程 CPU 时间只用于辅助定位计算和等待。系统调度造成的真实迟到仍计为期限失败。

跨进程或客户端的本地时钟未经对齐时不能直接相减。优先使用客户端记录的采样 tick、应用 tick、控制序号和明确的时间关联。

需要具体归因时再使用系统级线程运行、就绪和等待分析；它不是每轮验收的必跑步骤。

## 6. 后台、队列和容量

- 后台搜索或求解阻塞 500 ms 时，已有安全前段足够则不新增无谓停车；
- 请求、结果、输入账本、轨迹缓存和日志队列都声明最大条目数；大对象另声明字节或时间范围；
- 每种结构记录过期、替换、拒绝或背压行为；
- 饱和测试必须实际触发容量上限，证明不会阻塞控制线程或无限增长；
- 30 分钟运行用于发现泄漏和长期积压，不能代替静态容量和饱和测试。

## 7. 成功、恢复和重试

外部重新传送、重开任务或重复运行直到成功，算作新的任务样本，不能覆盖第一次失败。

任务内部允许的重新规划、有限候选搜索和受控恢复，可以计入任务成功，但必须发生在原任务的时间、资源和恢复次数预算内。报告：

- 首次无恢复完成率；
- 含合法恢复的任务成功率；
- 每次任务的恢复次数和代价；
- 每个失败的最终分类。

固定边界案例和随机样本分开报告。置信区间只用于事前定义的随机抽样分布，不给固定反例集合附加普遍概率解释。

## 8. 安全表述

B10 的安全结论统一写成：

> 在冻结版本、能力范围、风险策略和验收集合中，观察到的可避免安全违规为 0。

它不等于对所有地图、状态和未来扩展的绝对安全证明。未知放行、未授权伤害、错误完成和多输入写入都属于安全违规。

## 9. B15 前需要继续累计的记录

每个独立验收单元按规则分支、输入投影、控制策略和声明适用域登记。不能按文件数、类名或方块注册名凑模型数量。

入口域扩大、投影规则变化或动作机制变化时，登记哪些旧证据仍可复用及依据。多个已证明等价的普通材质可以共享主体样本，但要保留替换和边界验证。

## 10. 与既有验收门槛的关系

继续继承 [当前总架构](../architecture/mc_motion_navigation_architecture_v1.md)第 10 节中适用的门槛，包括唯一输入出口、旧请求／旧目标拒绝、取消与输入失联、控制帧时序、采样到输入实际应用、初次可用路线、后台阻塞和日志消费者阻塞。B10 文档没有重新抄写的要求不视为删除。

B10 替换的是连续动作阶段的交付顺序、动作求解证据、接续矩阵和本文件明确修订的统计口径。发生冲突时，以 D012 和本验收文档为准。

未知探索、动态实体、特殊材质、攀爬和流体不属于 B10 验收范围；它们继续由后续阶段单独取得能力和证据。

